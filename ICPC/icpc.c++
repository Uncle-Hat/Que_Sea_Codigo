#include <bits/stdc++.h>
using namespace std;

int divisionpar(int w){
    if (w % 2 == 0){
        true
    };
    else false;
}

int main() {
    ios::sync_with_stdio(0);cin.tie(0);cout.tie(0);
    int w;
    scanf("%d",&w);
    if divisionpar(w){
        printf("YES");
    };
    else{
        printf("NO");
    }
}