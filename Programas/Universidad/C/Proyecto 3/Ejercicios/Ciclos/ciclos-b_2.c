#include <stdio.h>
#include <stdbool.h>
int main(void)
{
    int i;
    bool res;
    printf("Colocale un valor a i :\n");
    scanf("%d", &i);
    i = 2 res = true 
    do
    {
        res = res && ()
    }
    while (i < x && res)
    printf("El valor final de i es : %d\n", i);
    return 0;
}
